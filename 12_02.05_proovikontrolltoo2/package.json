{
  "name": "dictionary-app",
  "version": "1.0.0",
  "scripts": {
    "start": "npm-run-all --parallel start-backend start-frontend",
    "start-backend": "cd dictionary && ./mvnw spring-boot:run",
    "start-frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "npm-run-all": "^4.1.5"
  }
}
